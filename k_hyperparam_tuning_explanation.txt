I have the starting directory, that has all the python files, and original data in it which I will create the feature engineered data from. The hyperparam_tuning_uptuna is the code I'm going to run. The hyperparams are defined, and the process_delta_t is going to start running over and over again for a hyperparam setup, for every delta_t value. The process delta t creates the labeled, feature engineered data and saves it in a folder a level below the file called labeled_data (Again, please please don't mess with the directories, I'm just explaining so you understand the context). Once these files are created, feature evaluation happens based on the labeled files. Once the best features are selected, and the data is pre processed, the training starts. Once the training is ready, the model is saved into a folder a level below the python file, called saved_models. The logging happens, the file is saved, and then comes the next delta_t, and this is repeated for all delta_t. If all the models are trained for delta_t, the model evaluation happens, and the best 5 (N_TOP_MODELS) models are stored, and the process repeats all over again with a different set of hyperparameters, untill every possible combination is exhausted. 